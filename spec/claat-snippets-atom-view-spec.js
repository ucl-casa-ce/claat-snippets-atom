'use babel';

import ClaatSnippetsAtomView from '../lib/claat-snippets-atom-view';

describe('ClaatSnippetsAtomView', () => {
  it('has one valid test', () => {
    expect('easy').toBe('easy');
  });
});
